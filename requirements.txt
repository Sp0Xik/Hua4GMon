# Зависимости Windows-версии (десктоп, main.py).
# Tkinter входит в стандартную библиотеку Python — отдельной установки
# не требует.
#
# Android-версия (android_main.py) собирается через Buildozer; её
# зависимости (kivy, pycryptodome и пр.) перечислены в buildozer.spec.
#
# Верхняя граница <2 — защита от мажорного релиза с ломающими
# изменениями API. Актуальная протестированная версия: 1.11.x.
huawei-lte-api>=1.10,<2
