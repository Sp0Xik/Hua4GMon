[tool.ruff]
target-version = "py310"
line-length = 100
exclude = ["dist", "build", ".venv"]

[tool.ruff.lint]
# Минимум полезных правил без агрессивных переписываний идиом:
#   E/W — pycodestyle (стиль)
#   F   — pyflakes (unused, undefined, etc.)
#   I   — isort (порядок импортов)
#   B   — flake8-bugbear (поведенческие баги, например mutable defaults)
select = ["E", "W", "F", "I", "B"]
ignore = [
    "E501",   # длина строки — мы переносим по смыслу, не по символам
    "B008",   # function calls in default args (часто нужно для Optional)
]

[tool.ruff.lint.isort]
# main.py и core — first-party модули проекта, не путать с third-party
known-first-party = ["main", "core"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]
# android_main: крипто-алиас Cryptodome->Crypto должен выполниться ДО
# импорта huawei_lte_api, поэтому часть импортов идёт после кода (E402).
"android_main.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
